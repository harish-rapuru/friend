== Fakebook (a facebook clone in rails)

Fakebook (a facebook clone in rails)

Setup
* clone repo
* bundle install
* rake db:migrate

Notes
* The keys are checked in to the repo, if you intend to deploy this to production, generate new keys.
